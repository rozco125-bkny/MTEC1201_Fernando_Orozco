/*
Name:  Fernando O
Title:  Warmup_Week4

*/

function setup() {
  createCanvas(700, 600);
}

function draw() {
  background("grey");

fill("red");
  strokeWeight(4);
    stroke("black");
  line(line30, 20, 85, 75);

function draw() {
  background(200);

  // Draw a vertical line that follows the mouse's x-coordinate.
  line(mouseX, 0, mouseX, 100);
}

}
